# taskflow

A minimal FastAPI project scaffold for task management.

## Structure

- `app/core/`
- `app/crud/`
- `app/models/`
- `app/routers/`
- `app/schemas/`
- `app/dependencies.py`
- `app/main.py`

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```
